import enviroment from './enviroment'
import paths from './paths'
import server from './server'
import webpack from './webpack'

// custom settings should be defined here in this folder
const settings = {
	enviroment,
	paths,
  server,
	webpack
}

export default settings
